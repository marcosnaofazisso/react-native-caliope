import React from "react";

import { Text, View, StyleSheet, TouchableOpacity, Button, FlatList, SafeAreaView } from "react-native";

export default function Menu({ navigation, route }) {

    return (
        <SafeAreaView>
            <View>
                <Text>Você está no Menu</Text>
            </View>
        </SafeAreaView>
    )
}
